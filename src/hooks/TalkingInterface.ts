export enum EnglishLevel {
  NATIVE = 'native',
  INTERMEDIATE = 'intermediate',
  BASIC = 'basic',
}

export enum LanguageCode {
  ES = 'es-ES',
  EN = 'en-US',
}

export enum Roles {
  USER = 'user',
  ASSISTANT = 'assistant',
}
